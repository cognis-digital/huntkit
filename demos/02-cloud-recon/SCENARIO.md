# Scenario: Cloud reconnaissance from compromised jump host

No matches against built-in Sigma rules — useful baseline to show clean scan.

## Expected findings

- Zero findings — extend with custom Sigma rules for cloud recon

## Why this matters

Demonstrates SENTRYLOG's extensibility: add custom rules under rules/.
